// =========================================
// 配置文件模板 / Configuration Template
// =========================================
// 使用说明 / Instructions:
// 1. 复制此文件并重命名为 credentials.h
// 2. 填入你的实际 WiFi 和服务器信息
// 3. credentials.h 已被 .gitignore，不会上传到 Git
//
// 1. Copy this file and rename to credentials.h
// 2. Fill in your actual WiFi and server information
// 3. credentials.h is gitignored and won't be uploaded

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi 配置 / WiFi Configuration
#define WIFI_SSID_STR "YOUR_WIFI_SSID"          // 替换为你的 WiFi 名称 / Replace with your WiFi name
#define WIFI_PASSWORD_STR "YOUR_WIFI_PASSWORD"  // 替换为你的 WiFi 密码 / Replace with your WiFi password

// 后端服务器配置 / Backend Server Configuration
#define BACKEND_HOST_STR "192.168.0.100"        // 替换为你的服务器 IP / Replace with your server IP
#define BACKEND_PORT_NUM 4000                   // 后端端口 / Backend port
#define BACKEND_PATH_STR "/api/snapshots"       // API 路径 / API path

// 🔒 API 认证密钥（可选，但强烈推荐）/ API Authentication Key (optional but highly recommended)
// 如果你的后端启用了 API 密钥验证，请在这里设置
// If your backend has API key validation enabled, set it here
// 留空则不发送 Authorization 头 / Leave empty to skip Authorization header
#define BACKEND_API_KEY_STR ""  // 例如 / Example: "your-secret-api-key-here"

#endif // CREDENTIALS_H
